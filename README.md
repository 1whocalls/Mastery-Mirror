# Mastery Mirror

## Install
In /client folder run
- npm install

In /server folder run
- npm install wrangler

## Develop
In /client folder run
- npm run dev

In /server folder run
- npx wrangler dev
