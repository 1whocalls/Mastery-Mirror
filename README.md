# Mastery Mirror

## Install
In /client folder run:

```bash
npm install
```

In /server folder run:

```bash
npm install wrangler
```

## Develop
In /client folder run:

```bash
npm run dev
```

In /server folder run:

```bash
npx wrangler dev
```
